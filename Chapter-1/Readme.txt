Working with strings:

const: Does nothing but promises that the value of the variable will never be changed. The value must be set at declaration time cause it is not possible to overwrite it.

String can be defined in one of three ways:

1. string st = "Hello";
2.string st(10,'c');
3.string st;

