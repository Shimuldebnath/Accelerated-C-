Looping and counting:

string::size_type cols=str.length();
The std::string type defines size_type to be the name of the appropriate type for holding the number of characters in
a string .

string::size_type : Normal integral type that can hold the size of any string.
size_t :unsigned integral type that can hold the size of any object
