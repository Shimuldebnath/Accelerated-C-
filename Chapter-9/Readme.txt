Chapter 9: Defining new types

Built in types: int ,char, float
Class types: string, vector, list

Class type is a mechanism for combining related data values into a data structure so that that structure can be treated as a single entity.

struct student_info{
  string name;
  double grade;
  vector<double> homework;
  
};

